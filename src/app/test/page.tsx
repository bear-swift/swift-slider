'use client'
import Slider from "react-slick";

const TestPage = () => {
  const settings = {
    className: "center",
    centerMode: true,
    centerPadding: "60px",
    slidesToShow: 2,
  };

  return (
    <div className="w-full">
      Hi
    </div>
  )
}

export default TestPage;